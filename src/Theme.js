import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#1d5fca"
        },
        secondary: {
            main: "#fe9d02"
        },
        default: {
            main: "#ffffff"
        }
    },
});

export default theme